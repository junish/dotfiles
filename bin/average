#!/usr/bin/env python

import sys

all = 0
sum = 0
for line in sys.stdin.readlines(): 
    nums = line.strip().split(' ')
    for n in nums:
        all += 1
        sum += int(n)
print float(sum)/all
